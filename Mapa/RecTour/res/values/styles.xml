<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="AppTheme" parent="android:Theme.Light" />

    <style name="nowCardStyle" parent="android:Widget.TextView">
        <item name="android:background">@drawable/search_bg_shadow</item>
        <item name="android:gravity">center</item>
        <item name="android:minHeight">200dp</item>
        <item name="android:minWidth">200dp</item>
        <item name="android:textColor">#707070</item>
    </style>
    
    
    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <color name="transparent">#00000000</color>
    <color name="fundo_translucido">#FFFECA11</color>

    <style name="TelaTransparente1">
        <item name="android:windowBackground">@color/fundo_translucido</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    
    
    <style name="TelaInfoBt"> 
        <item name="android:layout_margin">0dp</item>
        
    </style>
    
    
    <style name="InputsPortoLeve"> 
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:background">@drawable/inputs_selector</item>
    </style>
    
    
    <style name="TitulosFragment"> 
        <item name="android:background">@drawable/barra_cinza</item>
         <item name="android:textSize">18sp</item>
          <item name="android:gravity">center</item>
        
    </style>
    
     <declare-styleable name="TypeFaceButton">
        <attr name="nome_app" format="string" />
    </declare-styleable>
    
      <color name="Branco">#FFFFFF</color>
    <color name="Preto">#000000</color>

</resources>